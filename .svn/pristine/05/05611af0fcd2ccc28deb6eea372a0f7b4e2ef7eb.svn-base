#{extends 'CRUD/layout.html' /}
#{set title:messages.get('complaints') /}

<script>
$(document).ready(function() {
	$("tbody tr").each(function() {
		$(this).removeClass("odd").removeClass("even");
		$(this).find("td").each(function() {
			var link = $(this).find('.hiddenLinks').text();
			$(this).click(function() {
				$(location).attr("href", link);
			});
			$(this).hover(
			  function () {
			    $(this).addClass("hover");
			  },
			  function () {
			    $(this).removeClass("hover");
			  });
		});	
	});
});
</script>

<div id="crudList" class="${type.name}">

    <h2 id="crudListTitle">&{'complaints'}</h2>

    <div id="crudListSearch">
        #{crud.search /}
    </div>

    <div id="crudListTable">
        #{crud.table fields:['complaintName', 'complaintType'] }
           #{crud.custom 'complaintName'}
           	  <span class="hiddenLinks">@{Complaints.show(object.complaintId)}</span>
		      ${object.complaintName}
		   #{/crud.custom}
		   #{crud.custom 'complaintType'}
		      <span class="hiddenLinks">@{ComplaintTypes.show(object.complaintType.complaintTypeId)}</span>
		      ${object.complaintType.complaintTypeName}
		   #{/crud.custom}
		#{/ crud.table}
    </div>

    <div id="crudListPagination">
        &{'total', models.Complaint.count()}
        #{pagination /}
    </div>

    <p id="crudListAdd">
        <a href="@{blank()}">&{'complaints.add'}</a>
    </p>

</div>
