#{extends 'CRUD/layout.html' /}
#{set title:messages.get('objects.clinicalmanifestationnorms') /}

<div id="crudShow" class="${type.name}">

    <h2 id="crudShowTitle">&{'clinicalmanifestationnorm.edit'}</h2>

    <div class="objectForm">
    #{form action:@save(object._key()), enctype:'multipart/form-data'}
        #{crud.form fields:['description', 'updateDate', 'value']}
        	#{crud.custom 'updateDate'}
        		<label for="${object_updateDate}">&{'updateDate'}</label>
        		<input type="text" disabled="disabled" value=${object.updateDate.format()}>
        		%{date = new Date()}%
        		<input id="object_updateDate" type="hidden" name="object.updateDate" value=${date.format()}>
        	#{/crud.custom}
        #{/crud.form}
        <p class="crudButtons">
            <input type="submit" name="_save" value="&{'crud.save', type.modelName}" />
            <input type="submit" name="_saveAndContinue" value="&{'crud.saveAndContinue', type.modelName}" />
        </p>
    #{/form}
    </div>

    #{form @delete(object._key())}
        <p class="crudDelete">
            <input type="submit" value="&{'clinicalmanifestationnorm.delete'}" onclick="return confirm('&{'clinicalmanifestationnorm.delete'}')"/>
        </p>
    #{/form}

</div>